#include "main.h"

/**
 * str_len - returns the length of a string.
 * @str: string pointer.
 * Return: i.
 */

int str_len(char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)
	{
	}
	return (i);
}

/**
 * str_lenc - strlen function but applied for constant char pointer str.
 * @str: char pointer.
 * Return: i.
 */

int str_lenc(const char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)
	{
	}
	return (i);
}
